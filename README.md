## Some notes

1. I have now installed all my dependencies locally. I pulled this project onto a new computer and something changed, and I just don't think it should ever change unless I mean it to.
   * This was accomplushed using `pip install --terget . <packagename>`, where `--target/-t` changes the install location. Seems to just work, yay.
